package core_test

/*

import { series, step } from "./fizz-buzz"

test('11 is 11', () => {
    expect(step(11)).toBe(11)
})

test('12 is Fizz', () => {
    expect(step(12)).toBe('Fizz')
})

test('25 is FizzBuzz', () => {
    expect(step(25)).toBe('Buzz')
})

test('30 is FizzBuzz', () => {
    expect(step(30)).toBe('FizzBuzz')
})

test('from -10 to 10 are 21 items', () => {
    const values = series(-10, 10)

    expect(21).toBe(values.length)
})

test('from -10 to 0 first item is -10', () => {
    const values = series(-10, 0)

    expect(-10).toBe(values[0].step)
})

test('from 0 to 10 last item is 10', () => {
    const values = series(0, 10)

    expect(10).toBe(values.at(-1).step)
})

*/
